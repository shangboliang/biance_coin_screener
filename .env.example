# 币安POC监控工具 - 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# Telegram配置（可选，用于接收通知）
#TELEGRAM_BOT_TOKEN=
#TELEGRAM_CHAT_ID=

# Web访问控制配置（推荐设置）
# Web界面访问密码（强烈建议设置）
#WEB_PASSWORD=your_secure_password_here

# 会话超时时间（秒，默认3600=1小时）
#WEB_SESSION_TIMEOUT=3600

# IP白名单（可选，逗号分隔，例如：192.168.1.1,10.0.0.1）
#IP_WHITELIST=

# ==================== 代理配置 ====================
# 重要：根据服务器位置选择是否启用代理

# 国内服务器：需要代理访问币安和Telegram
# 取消下面的注释并设置为true
#BINANCE_USE_PROXY=true
#BINANCE_PROXY_HOST=127.0.0.1
#BINANCE_PROXY_PORT=7897
#TELEGRAM_USE_PROXY=true
#TELEGRAM_PROXY_HOST=127.0.0.1
#TELEGRAM_PROXY_PORT=7897

# 国外服务器：不需要代理（默认）
# 保持注释状态，或显式设置为false
#BINANCE_USE_PROXY=false
#TELEGRAM_USE_PROXY=false

# 使用说明：
# 1. 复制此文件: copy .env.example .env (Windows) 或 cp .env.example .env (Unix)
# 2. 填入实际的配置值
# 3. 如果不使用某项功能，可以不设置对应的环境变量


